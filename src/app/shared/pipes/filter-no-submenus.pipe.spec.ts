import { FilterNoSubmenusPipe } from './filter-no-submenus.pipe';

describe('FilterNoSubmenusPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNoSubmenusPipe();
    expect(pipe).toBeTruthy();
  });
});
