import { FilterWithSubmenusPipe } from './filter-with-submenus.pipe';

describe('FilterWithSubmenusPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterWithSubmenusPipe();
    expect(pipe).toBeTruthy();
  });
});
